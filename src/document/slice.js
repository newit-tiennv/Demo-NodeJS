/**
 * Lấy giá trị mong muốn của 1 array.
 * Etart: Vị trí bắt đầu lấy.
 * End: Vị trí cuối. Kết quả không bao gồm cái này.
 */

var arr = [1, 2, 3, 4, 5, 6, 7, 8];
const results = arr.slice(0, 4);
console.log(results); //=> [1, 2, 3, 4]