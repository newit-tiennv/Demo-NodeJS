/**
 * Kiểm tra 1 giá trị có trong mảng hay không.
 */

var animal = ["cat", "dog", "pig", "sheep"];

const checkDog = animal.includes("dog");
const checkMonkey = animal.includes("monkey");
console.log("Check dog: ", checkDog, "Check monkey: ", checkMonkey);